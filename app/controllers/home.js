var config    = require('../../config.json');

module.exports = {
  index: function (req, res, next) {
    // var data = {

    // };

    // res.render('index', data);
    res.render('index');
  }
};
